<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.DevMark" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/platinum</item>
        <item name="android:statusBarColor">@color/alabaster</item>
        <item name="android:windowBackground">@color/alabaster</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Theme.DevMark" parent="Base.Theme.DevMark" />
</resources>